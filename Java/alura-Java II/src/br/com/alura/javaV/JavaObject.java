package br.com.alura.javaV;

public class JavaObject {

	/*
	 * A classe object é a mãe de todas as classes que existem.
	 * Dela se herdam todos os métodos inerentes as classes.
	 * Extend implicito.
	 * Também é util para se referenciar a qualquer classe existente.
	 * O método toString() deve devolver informações sobre o estado 
	 * do objeto. É útil para a depuração no desenvolvimento. É boa 
	 * prática sobrescrever o método para dar um significado maior 
	 * do que a saída padrão desse método.
	 */
	
	
}
